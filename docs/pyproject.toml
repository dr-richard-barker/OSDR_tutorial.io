[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "ODSR tutorial"
authors = [{name = "Richard", email = "Richard.Barker@NASA.gov"}]
dynamic = ["version", "description"]